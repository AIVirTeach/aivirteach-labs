# aivirteach-server -> Agent (8770). Do not reuse the VM administration token.
AIVIRTEACH_AGENT_TOKEN="5df6cff8240365de0c2e0cff9afa5ea3f73fa7382f6ffe3c39a9f67cdc520384"
AIVIRTEACH_AGENT_HOST="127.0.0.1"
AIVIRTEACH_AGENT_PORT="8770"

# Allows the standalone Swagger page on 8780 to call the Agent from the browser.
AIVIRTEACH_AGENT_CORS_ORIGINS="http://127.0.0.1:8780,http://localhost:8780"

# Read-only processed course documents used to enrich server-provided steps.
AIVIRTEACH_COURSE_DIR="/opt/aivirteach-labs/.cache/course/AI Daily Briefing/processed"

# Agent -> read-only Diagnostic Gateway (8765). The same token is configured on
# that gateway, but AIVIRTEACH_API_TOKEN must never be present in the Agent unit.
AIVIRTEACH_GATEWAY_URL="http://127.0.0.1:8765"
AIVIRTEACH_DIAGNOSTIC_TOKEN="9cdf117d87bce82b34aef71ad7ddf297630987fcebf376f3d1d4f830aa5fbe82"

# fake is useful for API smoke tests. openai_compatible enables actual reasoning.
AIVIRTEACH_MODEL_PROVIDER="openai_compatible"
AIVIRTEACH_MODEL_BASE_URL="https://api.deepseek.com/"
AIVIRTEACH_MODEL_API_KEY="sk-591469cfcb1144adaaada731ae1dccfe"
AIVIRTEACH_MODEL_NAME="deepseek-v4-flash"
AIVIRTEACH_MODEL_THINKING="disabled"

AIVIRTEACH_AGENT_TOTAL_TIMEOUT="40"
AIVIRTEACH_MODEL_TIMEOUT="15"
AIVIRTEACH_TOOL_TIMEOUT="8"
AIVIRTEACH_MAX_REASONING_TURNS="4"
AIVIRTEACH_MAX_TOOL_CALLS="6"
AIVIRTEACH_MAX_TOOL_OUTPUT_CHARS="32768"
AIVIRTEACH_MAX_CONCURRENT_REQUESTS="4"
